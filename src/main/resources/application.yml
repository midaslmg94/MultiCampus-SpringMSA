server:
  port: 8888
spring:
  cloud:
     config:
       server:
         git:
           uri: https://github.com/midaslmg94/spring-config.git
           searchPaths: licensingservice, organizationservice
              #username: Private repo인 경우만 작성
              #password: 
